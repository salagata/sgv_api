const outputMessages = require("../effects.json");
const fs = require("fs");

function getEffecs(match = "") {
    return outputMessages;
}

function getEffectList(match = "") {
    
}

function getEffect(name) {
    
}